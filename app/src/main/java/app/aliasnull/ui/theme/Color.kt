package app.aliasnull.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Provisional AliasNull color tokens: a teal "terminal prompt" accent over a
 * near-black surface. These are a technical foundation, not the final identity.
 */
object AliasNullColors {
    // Light
    val LightPrimary = Color(0xFF006A5B)
    val LightOnPrimary = Color(0xFFFFFFFF)
    val LightPrimaryContainer = Color(0xFF74F8DF)
    val LightOnPrimaryContainer = Color(0xFF00201B)
    val LightSecondary = Color(0xFF006A60)
    val LightOnSecondary = Color(0xFFFFFFFF)
    val LightBackground = Color(0xFFFAFDFB)
    val LightOnBackground = Color(0xFF191C1C)
    val LightSurface = Color(0xFFFAFDFB)
    val LightOnSurface = Color(0xFF191C1C)

    // Dark
    val DarkPrimary = Color(0xFF51DBBC)
    val DarkOnPrimary = Color(0xFF00382E)
    val DarkPrimaryContainer = Color(0xFF005144)
    val DarkOnPrimaryContainer = Color(0xFF74F8DF)
    val DarkSecondary = Color(0xFF4CD9CB)
    val DarkOnSecondary = Color(0xFF003731)
    val DarkBackground = Color(0xFF191C1C)
    val DarkOnBackground = Color(0xFFE0E3E1)
    val DarkSurface = Color(0xFF191C1C)
    val DarkOnSurface = Color(0xFFE0E3E1)
}
